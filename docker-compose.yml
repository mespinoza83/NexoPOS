services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: nexopos
      POSTGRES_USER: nexopos
      POSTGRES_PASSWORD: nexopos_dev_password
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
volumes:
  postgres_data:
