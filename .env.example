# PostgreSQL
DATABASE_URL="postgresql://nexopos:nexopos_dev_password@localhost:5432/nexopos?schema=public"

# API
API_PORT=3001
WEB_ORIGIN="http://localhost:3000"
JWT_ACCESS_SECRET="replace-with-a-long-random-value"
JWT_REFRESH_SECRET="replace-with-a-different-long-random-value"

# Solo para crear los datos de desarrollo; no se guarda en la base de datos.
SEED_ADMIN_PASSWORD="use-a-unique-development-password"
